# Core OpenSpiel + BatchMCTS / PyTorch training dependencies.
pip >= 20.0.2
attrs >= 19.3.0
absl-py >= 0.10.0
numpy >= 1.26.0
scipy >= 1.10.1
ml-collections >= 0.1.1

# PyTorch (CUDA version; install separately for correct CUDA support)
# pip install torch --index-url https://download.pytorch.org/whl/cu121
# torch >= 2.0.0
