> 先不动valuehead了，我希望调整一下架构，value头能够输出 p1胜、和、p2胜
  三个状态。因为今后军棋游戏会有很多和棋的局面，如果想剪枝、避和等，都会需要和棋率评估。你分析一下这种状态输出的好处，评估一下如果要改成这种架构，有哪些地方需要改动。对现有架构的冲击大吗

● 好处：

  1. 和棋率显式建模——不再把 value≈0 混同为"均势"与"和棋导向"，MCTS 可以区分"胜率 50% 和棋率 0%"与"胜率 30% 和棋率 40%"两种完全不同局面
  2. 避和/求和策略——领先时知道对手能强和就不冒险，落后时知道还能求和就不弃子
  3. 训练信号更干净——one-hot target [1,0,0]/[0,1,0]/[0,0,1] 用 cross-entropy，没有 MSE 的"value=0 才是好"被逼和"value=±1 才是好"的矛盾
  4. 军棋天然需要——四国军棋和棋率可能 20%+，不懂和棋率的模型等于瞎子

  需要改动的地方：

  改动层次                   影响范围
  ──────────────────────────────────────────
  value_fc2: (32→1) → (32→3)    # 模型结构
  tanh → softmax/log_softmax      # 输出激活
  MSE → CrossEntropy              # 损失函数
  returns[0] → returns[cur]→class # buffer 存法
  node.outcome: 数值 → (W,D,L)   # MCTS solver
  evaluate(): [v,-v] → [w,d,l]   # MCTS evaluator 接口
  backprop: 期望值 = p_w·M - p_l·M (p_d·0)  # Q 值计算

  冲击评估：

  ┌───────────────┬───────────────────────────────────────────────────────────────────┐
  │     方面      │                               程度                                │
  ├───────────────┼───────────────────────────────────────────────────────────────────┤
  │ 模型架构      │ 小——只改 value_fc2 最后一个 Linear 的输出维度和激活函数           │
  ├───────────────┼───────────────────────────────────────────────────────────────────┤
  │ 训练流程      │ 中——loss 函数从 MSE 换 CE，buffer 存的 value 从 float 换 class id │
  ├───────────────┼───────────────────────────────────────────────────────────────────┤
  │ MCTS          │ 中——evaluate() 接口变，backprop 的 Q 计算从直接数值变期望值折算   │
  ├───────────────┼───────────────────────────────────────────────────────────────────┤
  │ 旧 checkpoint │ 不兼容——value_fc2 形状不同，需从头训练或写迁移脚本                │
  ├───────────────┼───────────────────────────────────────────────────────────────────┤
  │ 军棋          │ 零额外开销——和价值头直接共享 code path                            │
  └───────────────┴───────────────────────────────────────────────────────────────────┘

  建议：给 OthelloResNet 加 num_value_classes: int = 1 参数（1=旧版标量, 3=新版三分类），先保持默认 1 不破坏现有流程，军棋时传 3。这样改动小且不阻塞当前训练。